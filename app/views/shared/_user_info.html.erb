<%= link_to gravatar_for(current_user, size: Settings.gravatar_size),
  current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".linkto_profile_text"), current_user %></span>
<span>
  <%= t(".number_of_post",
        count: current_user.microposts.count) %>
</span>
